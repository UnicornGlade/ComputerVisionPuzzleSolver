у меня есть main.cpp файл (прилагаю), прочитай его полностью
в нем сейчас реализовано:
1) алгоритм AprilTags-подобное детектирование отрезков через итеративное объединение компонент связности
2) отладочная визуализация того как это происходит в алгоритме

пожалуйста напиши мне три новых файла find_segments.cpp/h/_test.cpp + main.cpp который использует этот алгоритм
алгоритм оставь неизменным, но вытащи его ввиде отдельной функции с алгоритмом который по картинке на вход - выдает множество отрезков:

1) должен быть простой способ настраивать его - сейчас это глобальные гиперпараметры,
я хочу чтобы они были с вариантом по умолчанию который сейчас, но чтобы легко было их настроить)
единственное что не должно быть внутри этой функции - это downscale картинки, он должен быть на уровне main.cpp
(т.к. координаты в результате работы функции должны соответствовать размерам входной картинки, т.е. она уже должна быть уменьшена)

2) должен быть простой способ указать папку для сохранения отладочных картинок внутри нее (с подпапками соответствующими итерациям),
если отладочная папка не указана (по умолчанию не указана) - не делать никакую отладочную визуализацию и не выводить сообщение [debug_io] magnitude medians

3) результат этой функции - список (std::vector) отрезков, каждый "отрезок" - это вектор точек point2i, в каждом отрезке так же сохрани информацию о:
- медиану силы градиента
- медиану того насколько отличаются силы градиенты от медианы градиента - т.е. от предыдущего пункта
- медиану направления (угла) градиента
- медиану того насколько отличаются (в градусах) градиенты пикселей отрезка от медианы направления (угла) градиента - т.е. от предыдущего пункта

4) в юнит-тесте создавай простые синтетические картинки (и обязательно сохраняй их визуализацию на диск, как входа так и выхода, чтобы я легко мог на них взглянуть)
в юнит-тесте в отдельную подпапку debug-unit-tests сохраняй визуализации как входных изображений, так и результатов

5) добавь так же в find_segments.h функцию визуализации отрезков - функцию которая на вход принимает изображение + список отрезков + параметры визуализации:
- порог какого минимального размера (по числу пикселей) отображать отрезки (те что меньше этого порога - не отображать, по умолчанию порог=2 пикселя)
- порог какой минимальной медианы силы градиента - отображать отрезки (те что меньше - не отображать, по умолчанию=0)
- порог какого максимального медианного отличия направления градиента отображать отрезки (те у которых отличие направлений больше порока - не отображать, по умолчанию=180 градусов)
- результирующая картинка - это цветная оригинальная картинка данная на вход, но более темная (в два раза) + наложенные сверху раскрашенные случайными цветами отрезки


переделай find_segments.cpp используя уже готовые функции из приложенных хэдеров (сейчас ты заново реализовала их дубликаты)
 - disjoint_set.h
 - fast_random.h
 - point2.h
 - runtime_assert.h
 - stats.h
 - gaussian_blur.h
 - grayscale.h
 - sobel_gradients.h
 - image.h
 - debug_io.h
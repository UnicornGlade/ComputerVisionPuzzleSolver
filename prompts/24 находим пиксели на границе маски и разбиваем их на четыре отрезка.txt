напиши find_mask_borders.h find_mask_borders.cpp find_mask_borders_tests.cpp

и реализуй функцию которая принимает на вход маску (0 - фон, 255 - объект интереса)
для каждого пикселя интереса (255) находит среди 8 соседей число фоновых и число пикселей интереса
соседи за пределами картинки (т.е. соседи крайних рядов) считаются фоном (т.е. как будто там был ноль во входной маске)
функция должна вернуть маску в которой 255 обозначены только такие пиксели которые в изначальной маске были пикселями интереса
и при этом у них среди 8 соседей нашлось хотя бы два соседа (вынеси это параметром-аргументом функции, но по умолчанию равен двум) которые в изначальной маске отмечены 0 (т.е. это приксели фона)
иначе говоря мы строим маску пикселей на границе объекта интереса

реализуй еще одну функцию, она принимает на вход маску объекта и построенную на основании нее маску пикселей на границе объекта интереса (0 - фон или внутри объекта, 255 - пиксель на границе объекта)
дальше разбивает пиксели на K отрезков - это стороны границы (мы знаем что граница - это полигон)
K - аргумент функции (по умолчанию равен 4, назвать можешь как-то получше, а не K)
алгоритм должен быть простой
- с помощью уже готового оператора Собеля (см. ниже) по маске объекта построить градиенты направленные внутрь объекта (т.к. фон в маске это 0, а объект - 255)
- перечислить списком ребра - смежные (по стороне или диагонали) пары пикселей такие что оба отмечены в маске границы объекта интереса
- отсортировать ребра по критерию "насколько у них отличается угол наклона градиента"
- бежать по этим ребрам и объединять в СНМ множества содержащие эти два пикселя (т.е. в первую очередь объединять те пары у которых похожее направление градиента)
- делать это до тех пор пока в СНМ больше чем K множеств
функция возвращает vector<vector<point2i>> (т.е. список K сторон границы, каждая сторона - это список пикселей)
при этом список пикселей упорядочен так что они обходят объект по часовой стрелке (у нас ведь есть градиенты)

реализуй еще одну функцию которая на основании списка K сторон границы (каждая сторона - список пикселей) и картинки-изображения (трехканальная, image8u)
для каждой из этих сторон нужно провести прямую из первого пикселя в последний (мы знаем что пиксели обходят объект по часовой стрелке)
еще один аргумент L (назови как-нибудь получше) - число точек сэмплирования
на этой прямой надо нанести указанное число L воображаемых точек сэмплирования
дальше для каждой точки сэмплирования нужно найти среди пикселей стороны два таких донора что их проекции на прямую содержат между собой эту точку сэмплирования
и теперь билинейно интерполировав два цвета этих двух пикселей из картинки-изображения - мы получаем цвет сэмплированной воображаемой точки
функция возвращает K векторов, в каждом векторе L сэмплированных цветов (создай point3u для удобного представления цветов)
обязательно в отладочной визуализации этой функции покажи где находятся точки сэмплирования (красным цветом) и где находятся парные точки-доноры (синим цветом)

в main.cpp добавь использование всех этих функций, и для каждой картинки-объекта создай картинку KxL размера (пусть по умолчанию гипер параметр L=10, K=4)
каждая строчка - одна из сторон объекта (и сэмплированные цвета, в порядке точек на стороне объекта по часовой стрелке)

прилагаю sobel_gradients.h для готового оператора Собеля
прилагаю disjoint_set.h для готового СНМ
прилагаю текущую версию main.cpp

еще для правильного результата я добавил в split_border_into_sides(...) блюр прежде чем считать Собелем градиенты (см. ниже код):
m = libimages::gaussian_blur_gray(m, 0.5f);
но перед тем как этот блюр делается - добавь padding - 10 черных пикселей отступа со всех сторон (чтобы еще улучшить результат)
но результат от этого (всмысле координат) не должен измениться, то есть это внутренний для функции padding для промежуточных вычислений

добавь отладочную визуализацию которая сохраняет эти изображения с padding-ом и рисует направления градиентов



добавь юнит-тест для ромба
добавь проверки что пикселей в каждой стороне правильное количество



ниже текущая версия split_border_into_sides
измени логику, вместо того чтобы просто создать список ребер и пройти по ним в порядке возрастания разницы углов
мне нужно два вложенных циклов
верхний перебирает какая разница углов допустима, изначально это разница углов от 0 до 15 градусов, потому от 15 до 30 градусов и так далее
вложенный цикл пробегает по ребрам и выполняет объединение только по тем парам пикселей между которыми разница углов в текщуем обрабатываемом допуске
а среди всех возможных кандидатов выбирает такое ребро, что одно из двух множеств в Disjoint Set наибольшее (относительно множеств которые участвуют в других допустимых сейчас парах-ребрах)
напиши мне новый вариант
std::vector<std::vector<point2i>> split_border_into_sides(const libimages::image8u& object_mask,
                                                         const libimages::image8u& border_mask,
                                                         int side_count,
                                                         SplitSidesDebugParams* dbg) {
...



перепроверь эту функцию split_border_into_sides и исправь баги или хотя бы упрости код, т.к. сейчас я вижу
unite: (0, 0) and (2, 0)
unite: (6, 0) and (8, 0)
и твою версию кода очень сложно отлаживать (выглядит как будто объединяются не смежные пиксели, в любом случае юнит-тест с ромбом падает - одна сторона забрала очень много пикселей)
...



спасибо, я отладил ромб с помощью твоего отладочного вывода и понял в чем дело
замени критерий с max(size(A), size(B)) на max(size(A), size(B))*1.0/min(size(A), size(B))
т.е. мы не хотим самое большое множество увеличить
грубо говоря мы хотим к самому большому множеству присоединить самое маленькое



этот критерий тоже плохо работает на реальных случаях
сделай вот так:
в каждом множестве поддерживай информацию о том какое среднее направление градиента в нем
объединяй тоже двухуровнево
но внешний цикл ограничивает размер допустимых множеств (изначально до 1 пикселя, затем до 2 пикселей, затем до 4 пикселей, затем до 8 пикселей, затем до 16 пикселей, и каждый раз порог увеличивается в два раза)
внутренний цикл выбирает среди ребер:
- такое ребро что объединенное множество не превысит ограничения на размер множества
- лучшим из них считается ребро у которого минимальная разница между средними направлениями объединяемых градиентов



добавь юнит тест который создает прямоугольник с по-разному сглаженными углами, с шумом, неровными краями и т.п.
обязательно проверь размеры найденных сторон



критерий все еще не работает, давай по-другому реализуем функцию
std::vector<std::vector<point2i>> split_border_into_sides(const libimages::image8u& object_mask,
                                                         const libimages::image8u& border_mask,
                                                         int side_count = 4,
                                                         SplitSidesDebugParams* dbg = nullptr);
известно что border_mask - это тонкая замкнутая фигура пикселей похожая на K-угольник, т.е. у каждого пикселя среди 8 соседей два отмечены этой маской
найди K углов (исходя из гипер-параметра что у нас K угольники)
и извлеки K сторон между ними
прежде чем писать код - предложи мне алгоритм

да, давай выделим цикл обходящий пиксели границы,
и будем выполнять в нем упрощения геометрии привносящие минимальную ошибку до тех пор пока не останется K сегментов
т.е. упрощение ломаной с минимальной ошибкой (reverse-RDP / Visvalingam-Whyatt-style), только на замкнутом цикле


текстовое описание алгоритма chatGPT:
полностью новая реализация split_border_into_sides(...) по твоей идее:
- извлекаем упорядоченный цикл пикселей границы из border_mask (тонкая замкнутая 8-связная линия)
- делаем упрощение цикла: каждый раз удаляем вершину, которая даёт минимальную локальную ошибку (distance-to-segment), пока не останется K=side_count вершин
- возвращаем K сторон как K кусочков исходного цикла между оставшимися вершинами, в порядке обхода по часовой стрелке (в image coords)

соседних вершин может быть больше двух - это все еще цепочка, но в некоторых местах может быть три соседа (например на угле или еще где-то)
кроме того может быть разрыв цепочки в один пиксель

ChatGPT: строим контур по object_mask классическим Moore-Neighbor tracing
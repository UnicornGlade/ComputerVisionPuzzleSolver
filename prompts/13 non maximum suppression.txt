напиши non_maximum_suppression.h + non_maximum_suppression.cpp + non_maximum_suppression_test.cpp
это должна быть функция которая принимает на вход картинку с градиентами (libimages::Gradients)
а возвращает результирующую картинку-маску image8u is_local_extremum:
 - 255 если пиксель является локальным экстремумом градиента
 - 0 если пиксель совпадает или меньше хотя бы одного из двух соседей

для этого в каждом пикселе из 8 соседних пикселей выбираем два соседа:
 - один по направлению нашего градиента
 - другой против направления нашего градиента
и сравниваем силу нашего градиента с их силами градиентов

еще добавь вспомогательную функцию которая применяет маску к libimages::Gradients - зануляет градиенты во всех пикселях где отмечен 0 в переданной маске image8u is_ok
еще добавь вспомогательную функцию которая строит маску "какие градиенты достаточно большие" (с учетом переданного порога magnitude)

заметка:
нейросеть набагала в pick_two_neighbors_from_dir()
ее же юнит-тесты это обнаружили т.к. упали
она делала скалярное произведение по диагонали не с единичным вектором а с вектором (-1,-1)
поэтому у диагональных смещений было преимущество в том чтобы быть выбраными как самое близкое к градиенту смещение
поправил нормализацией
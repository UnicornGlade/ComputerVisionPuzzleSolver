создай исходники utils.h utils.cpp utils_tests.cpp (gtest с визуализацией)
и создай bbox2.h bbox2.cpp bbox2_tests.cpp (по аналогии и используя point2f и point2i)
реализуй функцию которая по произвольному изображению
+ маске для этого изображения (маска такого же размера, черно-белая, только из 0 - игнорируй такие пиксели и 255 - пиксели интереса)
возвращает вектор из изображения+маски+смещение, каждая такая пара соответствует одному связному объекту интереса из оригинального изображения
один объект интереса - это связное множество пикселей отмеченных в исходной маске числом 255
связные множества определи используя DisjointSetUnion (см. ниже)
каждая из результирующих изображений+масок+смещение получается через определение bbox2 по пикселям одного из множеств,
при этом смещение - это координата в оригинальном изображении пикселя который в извлеченном результате находится по координатам (0, 0)

прилагаю disjoint_set.h
прилагаю point2.h

добавь в main.cpp (прикладываю текущий исходник ниже) еще один этап:
разбиение на объекты и сохранение каждого объекта отдельной картинкой+маской
но не вот так:
obj_0000
|-image.png
\-mask.png
obj_0001
|-image.png
\-mask.png
а в одну папку подряд вот так:
0000image.png
0000mask.png
0001image.png
0001mask.png